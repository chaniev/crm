# Требования: Клиенты

Доменный код: `CLI`. Источники миграции: `docs/archive/MVP-ТЗ.md`,
`docs/archive/BOT-МЕССЕНДЖЕР-ТЗ.md`, backlog/done (TASK-022, TASK-023,
TASK-027/070, TASK-068, TASK-079, TASK-115).

### REQ-CLI-001. Карточка клиента

**Решение:** принято

**Реализация:** реализовано

Для клиента хранятся: фотография, фамилия, имя, отчество, номер телефона,
дата рождения (необязательно), контактные лица (REQ-CLI-003), филиал,
группы, текущий абонемент с параметрами продажи (REQ-SUB-006), заметка
(REQ-CLI-005), история посещений, статус (REQ-CLI-002).

Обязательные поля при создании и редактировании: номер телефона и хотя бы
одно из полей ФИО. Клиент может быть создан без абонемента; абонемент
добавляется позже через карточку клиента.

**Границы и ограничения:**

- Отдельные ограничения не зафиксированы; применяются условия из текста требования.

**Связанные требования:** REQ-CLI-002, REQ-CLI-003, REQ-CLI-005, REQ-SUB-006

**Изменения:**

- 28.08.2026 — metadata переведена на раздельные поля решения и реализации (задача [TASK-134](../../backlog/implementation/TASK-134-requirements-workflow.md)).
- 27.08.2026 — новая (источник: MVP-ТЗ, TASK-022, TASK-079).

### REQ-CLI-002. Статус клиента

**Решение:** принято

**Реализация:** реализовано

Клиент активен или в архиве. Главный тренер, Супер-администратор и
Администратор переводят клиента в архив и возвращают обратно. Удаление
клиентов не требуется.

**Границы и ограничения:**

- Отдельные ограничения не зафиксированы; применяются условия из текста требования.

**Связанные требования:** нет

**Изменения:**

- 28.08.2026 — metadata переведена на раздельные поля решения и реализации (задача [TASK-134](../../backlog/implementation/TASK-134-requirements-workflow.md)).
- 27.08.2026 — новая (источник: MVP-ТЗ).

### REQ-CLI-003. Контактные лица

**Решение:** принято

**Реализация:** реализовано

До 2 контактных лиц. Тип — выбор из «мама»/«папа» или произвольный текст.
ФИО контактного лица — одним полем; обязателен номер телефона.

**Границы и ограничения:**

- Отдельные ограничения не зафиксированы; применяются условия из текста требования.

**Связанные требования:** нет

**Изменения:**

- 28.08.2026 — metadata переведена на раздельные поля решения и реализации (задача [TASK-134](../../backlog/implementation/TASK-134-requirements-workflow.md)).
- 27.08.2026 — новая (источник: MVP-ТЗ).

### REQ-CLI-004. Фотография клиента

**Решение:** принято

**Реализация:** реализовано

Хранится одна актуальная фотография; новая загрузка заменяет старую.
Файлы хранятся на стороне backend (не в БД; в БД только метаданные: путь,
MIME, размер, дата загрузки) в Docker volume. Допустимые форматы: JPEG, PNG,
WebP, HEIC, HEIF; недопустимые отклоняются с проверкой и расширения, и
MIME-типа; максимум 10 MB. HEIC/HEIF конвертируются в веб-совместимый формат.
Имя файла на диске генерируется backend. Рекомендуется уменьшение до 1024 px
по большей стороне. Доступ к фото проверяет backend: Главный тренер,
Супер-администратор и Администратор — все клиенты; Тренер — клиенты своих
групп (постоянные назначения и активные замещения).

**Границы и ограничения:**

- Отдельные ограничения не зафиксированы; применяются условия из текста требования.

**Связанные требования:** нет

**Изменения:**

- 28.08.2026 — metadata переведена на раздельные поля решения и реализации (задача [TASK-134](../../backlog/implementation/TASK-134-requirements-workflow.md)).
- 27.08.2026 — новая (источник: MVP-ТЗ).

### REQ-CLI-005. Заметка клиента

**Решение:** принято

**Реализация:** реализовано

В карточке клиента — одна рабочая заметка. Backend хранит автора и серверное
время последнего изменения; они видны в карточке. Редактировать могут
Администратор и выше; Тренер читает заметку только у клиентов своих групп.
Аудит-событие фиксируется только при фактическом изменении заметки.

**Границы и ограничения:**

- Отдельные ограничения не зафиксированы; применяются условия из текста требования.

**Связанные требования:** нет

**Изменения:**

- 28.08.2026 — metadata переведена на раздельные поля решения и реализации (задача [TASK-134](../../backlog/implementation/TASK-134-requirements-workflow.md)).
- 27.08.2026 — новая (источник: TASK-023, TASK-068).

### REQ-CLI-006. Дата рождения и возраст

**Решение:** принято

**Реализация:** реализовано

Необязательное поле «дата рождения»: вводится при создании, изменяется и
очищается в профиле. В карточке показывается дата рождения и возраст
(полные годы на текущую дату, вычисляется, не хранится). Права — как у
карточки клиента.

**Границы и ограничения:**

- Отдельные ограничения не зафиксированы; применяются условия из текста требования.

**Связанные требования:** нет

**Изменения:**

- 28.08.2026 — metadata переведена на раздельные поля решения и реализации (задача [TASK-134](../../backlog/implementation/TASK-134-requirements-workflow.md)).
- 27.08.2026 — новая (источник: TASK-079).

### REQ-CLI-007. Филиал и перевод клиента

**Решение:** принято

**Реализация:** реализовано

Клиент относится строго к одному филиалу. Нельзя записать клиента в группу
другого филиала. Перевод клиента в другой филиал прекращает текущий
абонемент (создаётся новая продажа в новом филиале; неиспользованный разовый
может переноситься) и исключает клиента из групп; опционально клиент сразу
записывается в группу нового филиала.

**Границы и ограничения:**

- Отдельные ограничения не зафиксированы; применяются условия из текста требования.

**Связанные требования:** нет

**Изменения:**

- 28.08.2026 — metadata переведена на раздельные поля решения и реализации (задача [TASK-134](../../backlog/implementation/TASK-134-requirements-workflow.md)).
- 27.08.2026 — новая (источник: TASK-022, TASK-031, TASK-070).

### REQ-CLI-008. Поиск и фильтрация клиентов

**Решение:** принято

**Реализация:** реализовано

Поиск по ФИО; поиск по телефону — только для Главного тренера,
Супер-администратора и Администратора. Фильтры: группа, статус
(активные/архивные), срок окончания абонемента, без фотографии, без группы,
без актуального оплаченного абонемента. Состав фильтров и вычисляемые
состояния определяет backend.

**Границы и ограничения:**

- Отдельные ограничения не зафиксированы; применяются условия из текста требования.

**Связанные требования:** нет

**Изменения:**

- 28.08.2026 — metadata переведена на раздельные поля решения и реализации (задача [TASK-134](../../backlog/implementation/TASK-134-requirements-workflow.md)).
- 27.08.2026 — новая (источник: MVP-ТЗ §11; состав уточнён после TASK-083 —
  фильтр признака оплаты упразднён вместе с ручной отметкой оплаты).

### REQ-CLI-009. Ролевой срез данных тренера

**Решение:** принято

**Реализация:** реализовано

Тренер видит только клиентов групп, где он назначен тренером (постоянно или
в активном замещении). По клиенту Тренер видит: ФИО, фотографию, свои группы
клиента, элементы отметки посещения, предупреждение о проблеме с абонементом
без раскрытия полных данных, индикатор отсутствия оплаты. Тренер не получает
через API телефон, контактных лиц, полные данные абонемента и суммы.
Проверка — на backend.

**Границы и ограничения:**

- Отдельные ограничения не зафиксированы; применяются условия из текста требования.

**Связанные требования:** нет

**Изменения:**

- 28.08.2026 — metadata переведена на раздельные поля решения и реализации (задача [TASK-134](../../backlog/implementation/TASK-134-requirements-workflow.md)).
- 27.08.2026 — новая (источник: MVP-ТЗ §2.2, BOT-ТЗ §3.1; замещения —
  TASK-073).
