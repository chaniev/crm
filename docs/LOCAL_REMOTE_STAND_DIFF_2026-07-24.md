# Различия локального и удалённого стендов на 2026-07-24

## Область сравнения

- Локальный стенд: `/Users/muradchaniev/Documents/work/crm`, ветка
  `codex/fix-regression-seed-deploy-20260724`.
- Базовый локальный commit: `f3fd8d63cda70dfa49c8c60a9012d1c0950651ce`.
- Локальное рабочее состояние включает исправления этой проверки; fingerprint diff и новых
  файлов относительно удалённой версии (без самого этого отчёта):
  `549bcf137c7e5e743c9ca4bff415cddb898c949b8f84345efccc5640a22e64a1`.
- Удалённый стенд до обновления: `84.54.59.17`, `/home/user/gym-crm`.
- Версия удалённых backend/frontend-образов:
  `58b650b879a90730d2681548dd18079f47fac4b4` (`58b650b-20260721-amd64`).

Сравнение выполнено до удаления удалённых volumes и зафиксировано резервной копией в
`/home/user/gym-crm/backups/pre-reset-20260724-0115`.

## Краткий список изменений

1. Локальное целевое состояние отличается от удалённой версии в 144 файлах исходников
   и документации (без этого отчёта): 136 отслеживаемых файлов
   (`+15162/-1154`) и 8 новых файлов (`+417` строк).
2. На удалённом стенде применена только миграция
   `20260513165936_InitialCreate`; локально применены также изменения начальной схемы и
   `20260721210111_FixClientMembershipVersionConstraints`.
3. Текущая `InitialCreate` после удалённой сборки была изменена. В ней появились новые
   таблицы, поля, внешние ключи и ограничения, которые не могут попасть в уже созданную
   удалённую БД простым повторным запуском миграций. Поэтому требуется чистое
   пересоздание БД.
4. Backend получил новые контракты и правила для стоимости/версий абонементов,
   комментариев, дат рождения, идемпотентности операций и бизнес-даты клуба.
5. Frontend получил соответствующие формы, API-мапперы, отображение стоимости,
   даты рождения, attention-панель и адаптивные исправления.
6. Bot-контракты обновлены под актуальный backend.
7. Полный seed актуализирован под текущую структуру, а для удалённого стенда добавлен
   отдельный admins-only seed.
8. Compose-файл локально и на сервере совпадает. Обновлять wiring сервисов не требуется;
   нужны новые образы, чистая БД и нормализация image-переменных в `.env`.

## Исходный код и контракты

Основные изменения между `58b650b` и локальным целевым состоянием:

- membership sale:
  - режимы ценообразования `Catalog`, `CatalogOverride`, `AmountOnly`;
  - хранение цены на уровне продажи;
  - целочисленные суммы в рублях и дополнительные check constraints;
  - комментарий к продаже и автор/время его изменения;
- membership versions:
  - исправленные уникальные индексы продажи и клиента;
  - overlap-ограничение действует только для актуальных версий;
  - отдельная таблица идемпотентности mutation-запросов;
- clients:
  - дата рождения;
  - автор и время изменения заметки;
  - актуализированные API-ответы, аудит и frontend-формы;
- business date:
  - mutation, refund, replay и attention-сценарии используют настроенную календарную
    дату клуба вместо смеси UTC и локального времени;
- frontend:
  - обновлены membership pricing UI/API;
  - устранён конфликт accessible name в карточке абонемента;
  - исправлена загрузка счётчика attention;
  - breakpoint журнала посещений поднят до `64rem`, чтобы исключить overflow на 768 px;
- test data:
  - seed-администраторы привязаны к филиалам;
  - 300 клиентов получают детерминированные даты рождения;
  - повторный seed очищает новые idempotency/audit-ссылки;
  - добавлен `seed-leninsky-admins-only.sh`, который создаёт только обязательный филиал
    и пять администраторов.

## Схема БД

| Параметр | Локальный стенд после чистого запуска | Удалённый стенд до обновления |
|---|---:|---:|
| `20260513165936_InitialCreate` | применена, актуальная версия | применена, старая версия |
| `20260721210111_FixClientMembershipVersionConstraints` | применена | отсутствует |
| Schema-only SHA-256 | `0429aa079493d2c756bd3ae240612937717a5fa6de4e11d93ce1f643b7e8d264` | `5a649d401a0b21a1e59211eeaa89062537606576c358a3212ad64ab17aa3da54` |

Изменения актуальной начальной схемы относительно удалённой:

- таблица `ClientMembershipIdempotencyRecords`;
- `Clients.BirthDate`, `NotesChangedByUserId`, `NotesChangedAt`;
- `ClientMembershipSales.PricingMode`, comment metadata и nullable catalog item;
- стоимость удалена из версии `ClientMembership` и закреплена за sale;
- новые индексы, FK и ограничения целых рублей;
- новая миграция исправляет индексы версий и условие пересечения периодов.

## Данные стендов на момент сравнения

| Сущность | Локальный стенд, полный seed | Удалённый стенд до обновления |
|---|---:|---:|
| HeadCoach | 1 | 1 |
| Administrator | 5 | 5 |
| Coach | 10 | 0 |
| Филиалы | 4 | 1 |
| Залы | 12 | 3 |
| Типы групп | 4 | 3 |
| Тренировочные группы | 30 | 3 |
| Клиенты | 300 | 5 |
| Клиенты с датой рождения | 300 | поле отсутствует в старой схеме |
| Элементы каталога | 1 системный | 12 |
| Абонементы клиентов | 0 | 5 |
| Посещения | 0 | 15 |
| Фотографии клиентов | 300 | 3 |

Полный локальный seed выполнен дважды; итоговые счётчики не изменились.

## Runtime и конфигурация

| Параметр | Локальный стенд | Удалённый стенд до обновления |
|---|---|---|
| Compose SHA-256 | `d149382b...aa65` | `d149382b...aa65` |
| Архитектура app-образов | `arm64` | `amd64` |
| Frontend port | `3000` | `80` |
| Backend | healthy | healthy |
| Frontend | healthy | healthy |
| PostgreSQL | healthy | healthy |
| Bot | healthy | контейнер отсутствовал |
| `BOT_ENABLED` | default `true` | `true`, но контейнер не был запущен |
| `ClientTelegram__Enabled` | `true` | `true` |

Удалённый `.env` содержал три последовательных набора `POSTGRES_IMAGE`,
`BACKEND_IMAGE`, `FRONTEND_IMAGE`, `BOT_IMAGE`. Docker Compose использовал последний
набор `58b650b-20260721-amd64`. При обновлении эти ключи нужно заменить одним набором,
а не дописывать четвёртый.

## Требуемые действия для обновления удалённого стенда

1. Собрать и экспортировать новые `linux/amd64` образы backend/frontend/bot вместе с
   `postgres:17-alpine`.
2. Проверить SHA-256 архива и передать image-only артефакты на сервер.
3. Остановить compose и удалить старые volumes.
4. Поднять только PostgreSQL.
5. До первого запуска backend выполнить новый admins-only seed. Он применит актуальную
   схему и создаст один обязательный филиал и пять администраторов.
6. Поднять backend/frontend/bot и проверить все health endpoints.
7. Подтвердить, что в БД ровно пять пользователей роли `Administrator`, нет
   `HeadCoach`, клиентов, групп и посещений; в каталоге остаётся только один системный
   элемент из `InitialCreate`.
8. Зафиксировать фактически развернутые image IDs, миграции и health-статусы.

## Результат чистого обновления 2026-07-24

- Перед удалением volumes создана и проверена резервная копия:
  `/home/user/gym-crm/backups/pre-reset-20260724-0115`.
- Удалённые PostgreSQL и photo/log volumes пересозданы с нуля.
- Развёрнуты `linux/amd64` образы с тегом
  `f3fd8d6-20260724-fix2-amd64`.
- Полный image-only архив этого тега экспортирован в `deploy/dist/`; его SHA-256
  проверен перед завершением работ.
- Фактические remote image IDs:
  - backend `sha256:e8ddeab1af8a174a45066c75c4caf130364a397becb7fd76d39f2f7b2bdd440c`;
  - frontend `sha256:cde88d62d8781f9c5889651c6e294c53336f4c46ad2efdfe27b64aa263dea0ad`;
  - bot `sha256:344d0040621d59a0488567e3e22845771284f3478b74b39c87a1f02c9f9a6ac4`;
  - PostgreSQL `sha256:af194ccf3e2d7fe367012c7b88ce8b816c5c889b18a5b316799a1f0d7eac746a`.
- Admins-only seed выполнен до первого запуска backend: создан один филиал
  «Ленинский» и пять пользователей роли `Administrator`.
- Итоговые счётчики удалённой БД:
  - users `5`, branches `1`, catalog items `1` системный;
  - halls, group types, groups, clients, memberships и attendance — `0`;
  - применены `20260513165936_InitialCreate` и
    `20260721210111_FixClientMembershipVersionConstraints`.
- DB, backend, frontend и bot имеют статус `healthy`; bot readiness возвращает
  `{"status":"ready"}`.
- Внешний frontend отвечает HTTP `200`. Порт backend `8080` закрыт внешним firewall,
  внутри compose backend healthy.
- Прямой маршрут к Telegram и настроенный proxy с удалённого сервера недоступны.
  Поэтому на удалённом стенде установлен `BOT_ENABLED=false`: bot-owned storage и
  health endpoints работают, Telegram polling не запускается. Локальный bot остаётся
  активным poller.
- Повторяющиеся image-переменные в удалённом `.env` нормализованы до одного набора.

## Проверки локального целевого состояния

- Backend: `260/260` тестов.
- Frontend: lint; `184/184` unit; production build; `85/85` Playwright e2e на
  отдельном порту.
- Bot: ruff; `40/40` pytest.
- Docker Compose: config validation, чистая сборка и запуск.
- Health: backend, frontend, frontend proxy и bot — healthy/ready.
- Full seed: два успешных последовательных запуска на чистом PostgreSQL.

Известное предупреждение, не блокирующее текущую выкладку: NuGet сообщает о
зарегистрированных уязвимостях `Magick.NET-Q8-AnyCPU 14.12.0`
(`NU1902`/`NU1903`). Это отдельное обновление зависимости, не причина падения тестов.
